   <h1>Waiters App</h1>
   
  
    <a href="/waiters"><button type="button" class="backBtn">Back</button></a>
        <a href="/clear"><button type="button" class="backBtn">Reset</button></a>
    <form action="/days" method="post">
      <div class="green">3 waiters</div>
   <div class="orange">0-2 waiters</div>
    <div class="red">more than 3 waiters</div>
       
    <div class="tables">
 <table>
    <thead>
  <tr>
     {{#each allDays}}
    <th style="background-color: {{this.color}}">{{this.chosen_day}}</th>
    {{/each}}
  </tr>
       <tr>
             {{#each allDays}}
             <td>
                {{#each this.users}}
                <ul>
                {{this.username}}
                </ul>
                {{/each}}
             </td>
             {{/each}}
       </tr>
</table>
</div>
</form>